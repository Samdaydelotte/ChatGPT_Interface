import pygetwindow as gw
import time
import sys

def closeApps():
    # try:
        # BROWSER = gw.getWindowsWithTitle("Edge")[0]
        # CODE = gw.getWindowsWithTitle("Visual Studio Code")[0]
    # except:
        # print("---Close.py\n---Open Edge")
        # sys.exit()
    # Closes all open apps
    # ActiveApplications = gw.getAllTitles()
    # for app_title in ActiveApplications:
    #     application = gw.getWindowsWithTitle(app_title)[0]
    #     application.minimize()

    # BROWSER.maximize()
    pass

    time.sleep(1)

if __name__ == "__main__":
    closeApps()

# closeApps()
    # Test